import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

// test('renders Guess UCLA heading', () => {
//   render(<App />);
//   const headingElement = screen.getByText(/Guess UCLA/i);
//   expect(headingElement).toBeInTheDocument();
// });
